# MCP Servers — Index

Model Context Protocol servers extend the AI OS with live connections to external tools.
Each MCP gives the AI the ability to read from and write to that tool directly.

| MCP | Purpose | Status | File |
|-----|---------|--------|------|
| [MCP Name] | [What it connects to] | [Active / Inactive] | [[mcp-name].md]([mcp-name].md) |

---

*Add a new row and file each time an MCP is installed.*
